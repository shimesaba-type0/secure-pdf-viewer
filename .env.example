# Flask設定
# シークレットキーの生成方法（Linux）:
# python3 -c "import secrets; print(secrets.token_hex(32))"
# または
# openssl rand -hex 32
FLASK_SECRET_KEY=your-secret-key-here
FLASK_ENV=development

# メール設定
MAIL_SERVER=your-mail-server.com
MAIL_PORT=465
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-email-password

# 管理者設定
ADMIN_EMAIL=admin@example.com

# システム設定
CLOUDFLARE_DOMAIN=your-domain.com

# PDF直接ダウンロード防止機能設定
PDF_DOWNLOAD_PREVENTION_ENABLED=true
# 許可するReferrer（ドメイン名、IPアドレス、CIDR、IP範囲対応）
# 例: localhost,127.0.0.1,yourdomain.com,10.0.0.0/24,192.168.1.1-192.168.1.100,.example.com
PDF_ALLOWED_REFERRER_DOMAINS=localhost,127.0.0.1,yourdomain.com,10.0.0.0/24
# ブロックするUser-Agent（主要言語のHTTPクライアント）
PDF_BLOCKED_USER_AGENTS=wget,curl,python-requests,urllib,httpx,aiohttp,Guzzle,cURL-PHP,Java/,Apache-HttpClient,OkHttp,node-fetch,axios,got,HttpClient,.NET Framework,Go-http-client,Ruby,faraday,httparty,reqwest,ureq,libcurl
PDF_USER_AGENT_CHECK_ENABLED=true
PDF_STRICT_MODE=false
PDF_LOG_BLOCKED_ATTEMPTS=true

